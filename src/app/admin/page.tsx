import React from "react";

export default function AdminPage() {
  return (
    <div>
      <h2 className="text-2xl font-bold">Admin Dashboard</h2>
      <p className="text-text-secondary">
        Protected admin area — implement RBAC before production use.
      </p>
    </div>
  );
}

# ─── Database ───────────────────────────────────────────────
# Main connection for runtime queries (Neon pooled)
DATABASE_URL="postgresql://user:password@aws.neon.tech/dragindrop?sslmode=require"
# Direct connection for schema migrations (Neon unpooled)
DATABASE_URL_UNPOOLED="postgresql://user:password@aws.neon.tech/dragindrop?sslmode=require"

# ─── Rate Limiting (Upstash Redis) ───────────────────────────
# Required for Vercel serverless. Get from Upstash dashboard.
UPSTASH_REDIS_REST_URL="https://your-upstash-url.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your_upstash_token"

# ─── Session ────────────────────────────────────────────────
SESSION_SECRET="generate-a-strong-random-string-here"

# ─── Email (Resend) ─────────────────────────────────────────
RESEND_API_KEY="re_your_api_key_here"

# ─── App ────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ─── OAuth: YouTube (Google) ──────────────────────────────────
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-your-secret"

# ─── OAuth: TikTok ────────────────────────────────────────────
TIKTOK_CLIENT_KEY="your-client-key"
TIKTOK_CLIENT_SECRET="your-client-secret"

# ─── Backblaze B2 Storage ─────────────────────────────────────
B2_ACCOUNT_ID="your-account-id"
B2_APP_KEY="your-app-key"
B2_BUCKET_NAME="your-bucket-name"
B2_ENDPOINT="https://s3.eu-central-003.backblazeb2.com"

# ─── Token Encryption (AES-256-GCM) ──────────────────────────
# Generate a random 64-char hex string (32 bytes):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
TOKEN_ENCRYPTION_KEY="your-64-char-hex-string"
